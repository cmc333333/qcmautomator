version: "3.7"
services:
  stocks:
    build: ./stocks
    environment:
      GCP_PROJECT_ID: qcmautomator
      APP_NAME: stocks
      GOOGLE_APPLICATION_CREDENTIALS: /app/creds.json
    ports:
    - "8001:8080"
    volumes:
    - "./stocks:/app"
